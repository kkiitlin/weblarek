# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные 

В приложении используются две сущности, которые описывают данные, — товар и покупатель. Их можно описать такими интерфейсами:

1. Интерфейс, описывающий данные о товаре:

```
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```

Свойства интерфейса:

`id: string` - уникальный идентификатор для каждого товара 
`description: string` - описание для каждого товара
`image: string` - картинка для каждого товара
`title: string` - название товара 
`category: string` - то, к какой категории относится товар 
`price: number | null` - стоимость товара (может иметь какую-то определенную стоимость, либо быть бесценным) 

2. Интерфейс, описывающий данные покупателя:

```
interface IBuyer {
payment: TPayment;
email: string;
phone: string;
address: string;
}
 ```

Свойства интерфейса:

`payment: TPayment`- способ оплаты (карта, наличные, пустое - тот случай, когда способ оплаты еще не выбран)
`email: string` - электронная почта покупателя 
`phone: string` - мобильный номер покупателя 
`address: string` - адрес покупателя 

Тип данных `TPayment`:

Тип данных, описывающий способ оплаты. Может принимать три фиксированных типа:
`"card": string` - покупатель выбрал тип оплаты "по карте"
`"cash: string"` - покупатель выбрал тип оплаты "за наличные"
`"": string` - пустое поле значения по умолчанию (пользователь еще не выбрал способ оплаты)

### Модели данных

#### Класс CatalogModel
Служит для хранения товаров, которые можно купить в преложении 

Конструктор: 

`constructor(protected events: IEvents)` - передается обработчик событий.

Поля класса:

`protected productsList: IProduct[]` - хранит массив всех товаров 

`protected choosenProduct: IProduct | null` - хранит товар, выбранный для подробного отображения

Методы класса:

`saveProductsList(productsList: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода

`getProductsList(): IProduct[]` - получение массива товаров из модели

`getIdProduct(id: string): IProduct | undefined` - получение одного товара по его id

`saveChoosenProduct(product: IProduct): void` - сохранение товара для подробного отображения и отправка события `product:select`

`getChoosenProduct(): IProduct | null` - получение товара для подробного отображения

#### Класс BasketModel
Служит для храния товаров, которые пользователь выбрал для покупки 

Конструктор: 

`constructor(protected events: IEvents)` - передается обработчик событий.

Поля класса:

`protected choosenShopList: IProduct[]` - хранит массив товаров, выбранных покупателем для покупки

Методы класса:  

`getBasketProduct(): IProduct[]` - получение массива товаров, которые находятся в корзине

`addProduct(item: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины

`removeItem(item: IProduct): void` - удаление товара, полученного в параметре из массива корзины
    
`clearBasket(): void` - очистка корзины

`getPrice(): number` - получение стоимости всех товаров в корзине

`getCountBasketItem(): number` - получение количества товаров в корзине

`hasProduct(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода

#### Класс BuyerModel
Служит для отображения данных покупателя, которые пользователь должен указать при оформлении заказа.

Конструктор: 

`constructor(protected events: IEvents)` - передается обработчик событий.

Поля класса:

`protected buyerData: IBuyer` - хранит такие данные, как адрес, способ оплаты, телефон, электронная почта.

Методы класса:  

`saveData(data: Partial<IBuyer>): void` - сохранение данных в модели.

`getUsersData(): IBuyer` - получение всех данных покупателя.

`clearUsersData(): void` - очистка данных покупателя.

`validate(): FormErrors` - валидация данных.

### Слой коммуникации 

#### Класс Communication
Класс будет использовать композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и будет получать с сервера объект с массивом товаров

Конструктор:

`constructor(api: IApi)` — принимает готовый экземпляр API.

Поля класса:

`protected api: IApi` - защищенное поле, хранящее экземпляр класса базового API для выполнения сетевых запросов.  

Методы класса:

`getProductList(): Promise<IProductResponse>` - делает get запрос на эндпоинт /product/ и возвращает массив товаров. 

`postData(order: IOrder): Promise<IOrderResult>` - делает post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода.

### Слой представения 

## Используемые классы 

#### Класс Header

Отображет корзину с товарами, а так же счетчик товаров.

Конструктор:

`constructor(container: HTMLElement, protected events: IEvents)` - передаем обработчик собитий и HTML элементы. 

Поля класса:

`busketButton:  HTMLButtonElement` - кнопка корзины.

`counterElem: HTMLElement` - счётчик товаров в корзине.

Методы класса:

`set counter(value: number)` - изменение счётчика товаров в корзине.

#### Класс Gallery

Класс реализует отображение товара на странеце.

Конструктор:

`constructor(container: HTMLElement)` - передается HTML элемент, в который помещается каталог товаров.

Методы класса:

`set catalog(products: HTMLElement[])` - сеттер, который служит для каталога товаровю

#### Класс Modal

Отображает в модальном окне различную информацию. 

Конструктор:

`constructor(container: HTMLElement, events: IEvents)` - принимает обработчик событий и HTML элемент, который в дальнейшем будет отображаться в модальном окне.

Поля класса:

`modalElem: HTMLElement` - элемент, который будет отображаться в модальном окне.

`closeButton: HTMLButtonElement` - кнопка, отвечающая за закрытие модального окна.

Методы класса:

`openWindow` - открытие модального окна.

`closeWindow` - закрытие модального окна.

`set content(item: HTMLElement)` - отвечает за контент, который показывается в модальном окне.

#### Класс Succeess

Класс, отвечающий за модальное окно после оформления заказа.

Конструктор:

`constructor(container: HTMLElement, protected events: IEvents)` - конструктор принимает обработчик событий и  HTML элемент, который будет отображаться в модальном окне.

Поля класса:

`successDescription: HTMLElement` - содержимое модального окна в момент подтверждения заказа.

`succeessButton: HTMLButtonElement` - кнопка для подтверждения заказа после оформления.

Методы класса:

`set counter(value: number)` - сеттер, обновляющий количество синапсов в зависимости от содержимого корзины.

#### Класс CardCatalog

Класс служит для отображения всех товаров в каталоге.

Конструктор:

`constructor(container: HTMLElement, events: IEvents)` - принимает обработчик событий и карточку товара (передаем HTMLElement).

Поля класса: 

`cardCategory: HTMLElement` - отвечает за категорию товара. 

`cardImage: HTMLImageElement` - отвечает за изображению. 

Методы класса: 

`set image(value: string)` - установка изображения товара.

`set category(value: string)` - устанавливает категорию товара. 

#### Класс CardPreview

Класс применяется для полного отображения карточки, вместе с полным описанием товара.

Конструктор: 

`constructor(container: HTMLElement, events: IEvents)` - принимает карточку товара и обработчик событий.

Поля класса:

`cardCategory: HTMLElement` - категория товара. 

`cardImage: HTMLElement` - изображение товара.

`cardDescription: HTMLElement` - полное описание товара.

`cardButton: HTMLButtonElement` - кнопка добавления/удаления товара из корзины.

Методы класса: 

`set image(value: string)` - установка изобрания товара

`set category(value: string)` - установка категории товара. 

`set description(value: string)` - уставка описания товара.

`set buttonText(value: string)` - установка текста кнопки товара.

#### Класс CardBasket

Класс, отвечающий за отображение содержимого корзины товаров.

Конструктор:

`constructor(container: HTMLElement, protected events: IEvents)` - в конструктор передается карточка товара, которая после отображается в корзини, помимо этого конструктор принимает обработчик событий

Поля класса:

`cardIndex: HTMLElement` - нумерация товаров в корзине.

`buttonRemove: HTMLButtonElement` - кнопка удаления товара из корзины.

Методы класса:

`set index(value: number)` - устанавливыает порядковый номер в корзине товаров.

#### Класс Basket

Контейнер для отображения товаров в корзине и для отображения интерфейса оформления заказа 

Конструктор:

`constructor(container: HTMLElement, protected events: IEvents)` - 
передается интерфейс корзины, а так же обработчик событий.

Поля класса:

`price: HTMLElement` - отображение общей стоимости товаров.

`basketButton: HTMLButtonElement` - кнопка оформления заказа. 

`basketList: HTMLElement` - контейнер для товаров, которые попадают в корзину.

Методы класса:

`set total(value: number)` - служит для установки общей стоимости всех товаров.

`set basket(items: HTMLElement[])`- служит для отображения списка товаров, которые попадают в корзину.

`set buttonStatus(value: boolean)` - служит для отображения состояния кнопки (выклчена в случае, если в корзине ничего нет).

#### Класс Order

Отвечает за форму, в которой выбирается способ оплаты и заполняется поле с адресом.

Конструктор:

`constructor(container: HTMLElement, protected events: IEvents)` - передается обрабочик событий, а так же элемент, отвечающий за интерфейс формы.

Поля класса:

`buttonOnline: HTMLButtonElement`- кнопка для оплаты по карте.

`buttonOfflie: HTMLButtonElement` - кнопка для оплаты за наличные.

`address: HTMLInputElement` - инпут для ввода адреса. 

Методы класса:

`payment(value: TPayment)` - сеттер, отвечающий за выбор способа оплаты.

`addressInput(value: string)` - сеттер, отвечающий за установку адресса пользователя. 

### Класс Contacts

Класс реализует ввод данных пользователя (email и номер телефона) в моадльном окне.

Конструктор:

`constructor(container: HTMLElement, protected events: IEvents)` - 
передается обработчик событий, а также элемент, отвечающий за интерфейс формы ввода контактных данных.

Поля класса:
 
`emailInput: HTMLInputElement` - инпут для ввода email.

`phoneInput: HTMLInputElement` - инпут для ввода мобильного телефона.

Методы класса:

`set email(value: string)` - сеттер, который устанавливает электронную почту.

`set phone(value: string)` - сеттер, который устанавливает номер мобильного телефона.

## Переиспользуемые сущности 

Слой представления обладает такими классами, которые отвечают за формы данных, а так же за карточку товара в различных вариациях. Для того, чтобы убрать повтор реализации методов, вынесли это в отдельные классы

#### Класс Card

Родитель для классов `CardCatalog`, `CardPreview`, `CardBasket`

Поля класса:
 
`cardTitle`: HTMLElement` - название самого товара.

`cardPrice`: HTMLElement` - цена товара.

`idCard?: string` - id товара.

Конструктор класса:

`conructor(container: HTMLElement)` - передаем в конструктор HTMLElement (такие элементы, которые являются общими для дочерних классов).

Методы класса:

`set title(value: string)` - сеттер, который необходим для установки названия товара.

`set price(value: number | null)` - сеттер, который необходим для установки стоимости товара.

`set id(value: string)` - сеттер, который необходим для установки id товара.

#### Класс Form

Родитель для классов `Order`, `Contact`

Конструктор: 

`constructor(container: HTMLElement, protected events: IEvents)` - передаеся HTML элемент (элементы, которые являются общими для дочерних классов) и обработчик событий.

Поля класса:

`errorForm: HTMLElement` - поле, отвечающее за ошибки при заполнении формы.

`buttonForm: HTMLButtonElement` - поле, отвечающее за конпку подтверждения в форме.

Модель класса:

`set error(value: string)` - отвечает за установку ошибок формы.

`set buttonStatus(value: boolean)` - отвечает за отображение состояния кнопки.

` protected onInputChange(field: keyof T, value: string)` - генерация события.

### Презентер

Презентер реализован в `src/main.ts` — точке входа приложения. Отдельного класса у него нет: его роль выполняют подписки на события моделей и представлений, в обработчиках которых презентер берёт данные из моделей и передаёт их в представления. Вспомогательная логика вынесена в функции модуля: `getPreviewButtonText`, `updateFormsState`, `syncPaymentState`, `renderOrderForm`, `renderContactsForm`.

Список обрабатываемых событий:

- `catalog:change` — выводит карточки товаров в галерею;
- `card:click` — находит товар по `id` и сохраняет его в `CatalogModel` как выбранный;
- `product:select` — показывает превью товара в модальном окне и задаёт текст кнопки («Купить», «Удалить из корзины» или «Недоступно»);
- `card__preview:click` — добавляет товар в корзину или удаляет его из неё и закрывает модальное окно;
- `basket:change` — обновляет счётчик товаров в шапке, список карточек корзины и итоговую сумму;
- `basket:open` — открывает корзину в модальном окне;
- `card__basket:remove` — удаляет товар из корзины;
- `basket:success` — открывает форму заказа `Order`;
- `form:change` — сохраняет изменённые пользователем данные в `BuyerModel`;
- `buyer:change` — выводит ошибки в формах `Order` и `Contacts` и управляет доступностью их кнопок (кнопка заблокирована, пока форма невалидна);
- `order:submit` — при валидной форме заказа открывает форму контактов `Contacts`;
- `contacts:submit` — отправляет заказ на сервер через `Communication.postData`, очищает корзину и данные покупателя и показывает окно успешного заказа с суммой из ответа сервера;
- `succeess:agree` — закрывает модальное окно после успешного оформления.

Последовательность оформления заказа: корзина → «Оформить» → форма заказа (способ оплаты и адрес) → «Далее» → форма контактов (email и телефон) → «Оплатить» → сообщение об успешном заказе.
